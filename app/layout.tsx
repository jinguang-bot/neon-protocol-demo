import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "Neon Protocol - Find Manufacturing Experts Instantly",
  description: "The First Agent-to-Agent Protocol for Professional Knowledge Trading",
  keywords: "manufacturing, experts, AI agents, knowledge trading, semiconductor, supply chain",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
